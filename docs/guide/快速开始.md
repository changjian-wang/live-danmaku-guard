# 快速开始

本指南将帮助你快速运行 LiveDanmakuGuard 项目。

## 📋 环境要求

### 必需软件

- **.NET 8.0 SDK** - [下载地址](https://dotnet.microsoft.com/download/dotnet/8.0)
- **Node.js 18+** (推荐 20.x) - [下载地址](https://nodejs.org/)
- **Git** - [下载地址](https://git-scm.com/)

### 可选软件

- **Visual Studio 2022** 或 **VS Code** - 代码编辑器
- **Docker Desktop** - 容器化部署

## 🚀 快速启动

### 1. 克隆项目

```bash
git clone https://github.com/changjian-wang/live-danmaku-guard.git
cd live-danmaku-guard
```

### 2. 启动后端

```bash
# 进入后端目录
cd src/backend

# 恢复NuGet包
dotnet restore

# 运行API项目
dotnet run --project LiveDanmakuGuard.Api
```

后端API将在 `http://localhost:5000` 启动。

访问 `http://localhost:5000/swagger` 查看API文档。

### 3. 启动前端

打开新的终端窗口：

```bash
# 进入前端目录
cd src/frontend/live-danmaku-guard-web

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端应用将在 `http://localhost:5173` 启动。

### 4. 访问应用

在浏览器中打开 `http://localhost:5173` 即可使用应用。

## 🐳 使用Docker (可选)

### 方式一：Docker Compose (推荐)

```bash
# 在项目根目录执行
docker-compose up -d
```

服务将在以下地址启动：
- 前端: `http://localhost:8080`
- 后端API: `http://localhost:5000`

### 方式二：手动构建

```bash
# 构建镜像
docker build -t live-danmaku-guard:latest .

# 运行容器
docker run -d -p 5000:8080 --name api live-danmaku-guard:latest
```

## 🧪 运行测试

### 后端测试

```bash
cd src/backend
dotnet test
```

### 前端测试

```bash
cd src/frontend/live-danmaku-guard-web
npm run test
```

## 📝 常见问题

### 端口已被占用

如果5000或5173端口已被占用，可以修改配置：

**后端端口修改**:
编辑 `src/backend/LiveDanmakuGuard.Api/Properties/launchSettings.json`

**前端端口修改**:
编辑 `src/frontend/live-danmaku-guard-web/vite.config.ts` 中的 `server.port`

### 依赖安装失败

**前端依赖问题**:
```bash
# 清除缓存重新安装
rm -rf node_modules package-lock.json
npm install
```

**后端依赖问题**:
```bash
# 清除NuGet缓存
dotnet nuget locals all --clear
dotnet restore
```

### 数据库问题

如果遇到数据库相关错误：

```bash
# 删除现有数据库
rm src/backend/LiveDanmakuGuard.Api/app.db

# 重新运行应用会自动创建数据库
dotnet run --project src/backend/LiveDanmakuGuard.Api
```

## 🔧 开发工具推荐

### Visual Studio Code 扩展

- **C# Dev Kit** - C#开发支持
- **Volar** - Vue 3开发支持
- **ESLint** - 代码检查
- **Prettier** - 代码格式化
- **GitLens** - Git增强
- **Thunder Client** - API测试

### 浏览器扩展

- **Vue.js devtools** - Vue调试工具
- **React Developer Tools** - 通用前端调试

## 📚 下一步

- 查看 [开发环境搭建](../development/开发环境搭建.md) 了解详细配置
- 阅读 [技术架构](../plan/02-技术架构.md) 了解项目结构
- 参考 [API设计](../plan/05-API设计.md) 了解接口规范

## 🆘 获取帮助

如果遇到问题：

1. 查看 [Issues](https://github.com/changjian-wang/live-danmaku-guard/issues)
2. 提交新的 Issue
3. 查看项目文档

---

**最后更新**: 2026-01-04
